package jp.andpad.api.domain

/**
 * 工事案件予算の承認・編集状態を表す列挙型。
 *
 * 予算の編集可否、承認ワークフロー、原価計上の基準となる
 * 予算バージョンの状態管理に使用する。
 */
enum class BudgetStatus {
    /** 下書き：編集中。未承認のため原価計上の基準にはならない */
    DRAFT,

    /** 承認済み：正式に承認され、原価管理の基準予算として有効 */
    APPROVED,

    /** ロック：編集不可。承認後の改定を防止する */
    LOCKED
}
